import React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div style={{ margin: "var(--outsidePadding)" }}>
      <h1>Not found!</h1>
      <p>Sorry. There’s nothing here.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
